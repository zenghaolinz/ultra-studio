# Ultra Studio v0.5.3 Stabilization Implementation Plan

**Goal:** Repair reproducible startup and desktop bridge failures, publish version `0.5.3`, and synchronize verified delivery materials.

**Architecture:** Keep the existing React, Tauri, and FastAPI boundaries intact. Apply narrow fixes at process startup and HTTP response handling boundaries, then update release metadata and mirror the verified source tree into the delivery backup.

**Tech Stack:** React, TypeScript, Vite, Tauri 2, Rust, FastAPI, Python

---

### Task 1: Establish Verification Baseline

**Files:** No source edits.

**Steps:**

1. Run `npm run check`.
2. Run `npm run build`.
3. Run `python -m compileall -q sidecar scripts`.
4. Run `cargo check --manifest-path src-tauri/Cargo.toml`.

### Task 2: Repair Python Startup Boundaries

**Files:**

- Modify: `sidecar/__main__.py`
- Modify: `sidecar/tools/comfyui_manager.py`

**Steps:**

1. Reproduce the broken module entry point with `python -m sidecar`.
2. Remove the stale `init_sidecar_db` import.
3. Preserve Windows portable ComfyUI executable and script paths as argument-list values so paths containing spaces launch correctly.
4. Verify module import and structured launch arguments.

### Task 3: Harden Tauri Sidecar Bridge

**Files:**

- Modify: `src-tauri/src/commands/sidecar.rs`

**Steps:**

1. Treat only successful health responses as initialized Sidecar state.
2. Add Unicode-safe response excerpts for parse error reporting.
3. Replace byte-index excerpts across generated asset response parsing.
4. Add a unit test covering multibyte truncation.
5. Run `cargo test --manifest-path src-tauri/Cargo.toml`.

### Task 4: Release And Delivery Synchronization

**Files:**

- Modify version metadata in npm, Tauri, Rust, and FastAPI manifests.
- Modify developer documentation for v0.5.3.
- Sync source files to `UltraStudio_提交材料_20260519/新建文件夹/决赛版本/完整代码`.

**Steps:**

1. Set application version to `0.5.3`.
2. Record fixed behavior and verification in developer documentation.
3. Run all verification commands again.
4. Copy verified source and documentation into the delivery backup while excluding runtime data and generated build caches.
